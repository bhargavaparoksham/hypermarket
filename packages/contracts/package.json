{
  "name": "@hypermarket/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'contracts package not bootstrapped yet'",
    "lint": "echo 'contracts lint not configured yet'",
    "test": "echo 'contracts tests not configured yet'"
  }
}
